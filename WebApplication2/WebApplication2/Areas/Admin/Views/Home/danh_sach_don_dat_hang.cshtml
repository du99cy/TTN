@model IEnumerable<WebApplication2.Database.dat_hang>
@{
    ViewBag.Title = "danh_sach_don_dat_hang";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayoutPage.cshtml";
}

<h2>danh_sach_don_dat_hang</h2>

<div>
    <h2>Tìm</h2>
    <form method="post">
        <input type="text" name="search" id="search" />
        <button type="submit">Tìm</button>
    </form>
</div>
<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>

        <th>
            @Html.DisplayNameFor(model => model.id_don_dat_hang)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.id_khach_hang)
        </th>

        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>

            <td>
                @Html.DisplayFor(modelItem => item.id_don_dat_hang)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.id_khach_hang)
            </td>

            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "tim_kiem_thong_tin", new { id_khach_hang = item.id_khach_hang, id_don_hang = item.id_don_dat_hang }) |
                @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
            </td>
        </tr>
    }

</table>


